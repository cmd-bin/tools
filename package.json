{
  "name": "react-native-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "@nx/js": "^23.0.1",
    "nx": "^23.0.1"
  }
}
